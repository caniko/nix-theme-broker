# Mixed native/generated targets

One selection may use a native Neovim adapter and a generated Stylix
Alacritty target. The broker resolves each managed target independently while
keeping one provider/variant selection.
